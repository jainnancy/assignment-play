# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.HomeController.index

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file              controllers.Assets.versioned(path="/public", file: Asset)

GET     /signup                    controllers.HomeController.signup

POST    /createUser                controllers.SignupController.storeData()

GET     /getUser                   controllers.SignupController.getData(username: String)

GET     /login                     controllers.HomeController.login

POST    /userhome                  controllers.LoginController.userhome

POST    /adminhome                 controllers.LoginController.adminhome

GET     /resetpassword             controllers.LoginController.resetpassword
